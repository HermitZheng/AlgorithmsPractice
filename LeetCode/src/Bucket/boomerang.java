package Bucket;

/**
 * @author zhuqiu
 * @date 2020/5/17
 */
public class boomerang {

    public int numPoints(int[][] points, int r) {
        int len = points.length;
        for (int i = 0; i < len; i++) {
            int x = points[i][0];
            int y = points[i][1];

        }
        return 0;
    }
}
